# data-poll
this script is to collect snaps form NE list then store it in classified order 

is a mutiple shell scripts calling each other to do the task , so that all of the shell scripts should be located at same file location
strat.sh : doing the main funcation , and mangeing the other scripts
check.sh : do some side checks 
cli.sh : to run the required commands , 

you need ip.txt for all the list of neede NEs to be checked 
